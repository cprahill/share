# BUILD 137 — local top-3 high scores

Lives in the browser (`rdb-board-v2`). Top 3 per planet × mode. 4-letter tags. Same name keeps the better run. Auto-saves at the finish; SAVE again if you change the tag.
