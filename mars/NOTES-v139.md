# BUILD 139 — mobile landscape + visual viewport

Safari on iPhone cannot hide the URL bar in a tab. The game pins canvas/HUD/home to `visualViewport` so the course fills the visible area. Portrait shows TURN YOUR PHONE. Add to Home Screen (manifest, standalone, landscape) is the chrome-free app.
